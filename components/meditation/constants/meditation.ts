export const GROUP_DURATION_SECONDS = 4 * 60;
export const PRESENT_WINDOW_SECONDS = 3 * 60;
export const PERSONAL_DURATION_SECONDS = 4 * 60;

export const AUDIO_PLAYER_OPTIONS = {
  downloadFirst: true,
  updateInterval: 500,
};
